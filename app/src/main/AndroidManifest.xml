<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.donacare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Donacare">
        <activity android:name=".UI.DetailKelasActivity"></activity>
        <activity android:name=".UI.InputDanaActivity" />
        <activity android:name=".UI.DetailHome" />
        <activity android:name=".UI.SplashActivity"></activity>
        <activity android:name=".UI.InputDonasiActivity"></activity>
        <activity android:name=".UI.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.RegisterActivity" />
        <activity android:name=".UI.LoginActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>